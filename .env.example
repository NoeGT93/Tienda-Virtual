HOST=127.0.0.1
PORT=8787
DATABASE_PATH=./data/lulos.sqlite
# Set NODE_ENV=production only behind HTTPS and a persistent Node host.
NODE_ENV=development
# Use an exact HTTPS origin in production.
PUBLIC_ORIGIN=http://127.0.0.1:8787
